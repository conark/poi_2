<form class="box" action="/editplace/{{place._id}}/addreview" method="POST">
  <label>Enter new review</label>
  <div class="field is-horizontal">
    <div class="field-body">
      <div class="field">
        <input class="input" type="text" placeholder="Enter Rate" name="rate">
      </div>
      <div class="field">
        <input class="input" type="text" placeholder="Enter Review" name="review">
      </div>
      <button class="button is-primary">Add Review</button>
</form>
{{!-- <form action="/editplace/{{place._id}}/uploadimage" method="POST" enctype="multipart/form-data">
      <div id="file-select" class="file has-name is-fullwidth">
        <label class="file-label"> <input class="file-input" name="imagefile" type="file" name="resume"
            accept="image/png, image/jpeg">
          <span class="file-cta">
            <span class="file-icon">
              <i class="fas fa-upload"></i>
            </span>
            <span class="file-label">
              Choose a file…
            </span>
          </span>
          <span class="file-name"></span>
        </label>
          <button class="button is-primary">Add Review</button>
      </div>
    </form>
  </div>
</div> --}}

<script>
  const fileInput = document.querySelector(".file-input");
  fileInput.onchange = () => {
    if (fileInput.files.length > 0) {
      const fileName = document.querySelector(".file-name");
      fileName.textContent = fileInput.files[0].name;
    }
  };
</script>